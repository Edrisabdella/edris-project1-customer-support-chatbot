# Final Upload Note

Before uploading to Udacity, complete the AWS evidence capture steps in `03_EVIDENCE/README_CAPTURE_REQUIRED.md` and add the real screenshots plus the real `output_eval_dataset.jsonl`.

The current package intentionally does **not** fabricate AWS screenshots or evaluation scores. This protects the submission from being rejected for unverifiable evidence.
